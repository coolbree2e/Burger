<h1 class="head">Burgers!</h1>

<h2 class="head">BurgerTime!</h2>

<ul>
  {{#each burgers}}
    {{!-- {{#unless devoured}} --}}
      {{!-- {{> burgers/burger-block devoured=true}} --}}
    {{!-- {{/unless}} --}}
  {{/each}}
</ul>



{{!-- <ul>
  {{#each cats}}
    {{#if sleepy}}
      {{> cats/cat-block sleep=false}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2 class="form-group">Add any burger you want</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger: </label>
    <input type="text"  id="box" name="name">
  </div>

  

  <button type="submit">Add da Burger</button>


  
</form>
